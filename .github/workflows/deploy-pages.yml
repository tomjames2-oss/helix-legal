name: Deploy HELIX legal pages

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Pages must be enabled manually first
        run: |
          echo "GitHub Pages must first be enabled in Settings > Pages."
          echo "Choose Deploy from a branch, branch main, folder / (root)."
